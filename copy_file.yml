---
# This playbook deploys the whole application stack in this site.

- name: Copy a new "pippo.conf file into place, backing up the original if it differs from the copied version
  copy:
    src: /pippo.conf
    dest: /root/pippo.conf
    owner: root
    group: root
    mode: '0644'
    backup: yes
